// Archivo: scripts.js
$(document).ready(function () {
    // Personalización del carrusel (opcional)
    $('#carouselExample').carousel({
        interval: 3000, // Cambia automáticamente cada 3 segundos
        wrap: true,     // Permite rotar continuamente
        pause: 'hover'  // Pausa el carrusel al pasar el ratón
    });
});
